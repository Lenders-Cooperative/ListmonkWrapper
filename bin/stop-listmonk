#!/usr/bin/env bash
set -euo pipefail

COMPOSE_FILE="docker-compose.listmonk.yml"

echo "[stop-listmonk] Stopping Listmonk..."
docker compose -f "${COMPOSE_FILE}" down -v

echo "[stop-listmonk] Done."
